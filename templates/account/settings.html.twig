{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}

<div class="container mx-auto py-5">
    {{ form_start(form) }}
        {{ form_row(form.pseudo) }}
        {{ form_row(form.oldPassword) }}
        {{ form_row(form.password, {
            label: 'Mot de passe'
        }) }}

        <button type="submit" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline w-full" formnovalidate>Modifier les informations</button>
    {{ form_end(form) }}
</div>

{% endblock %}
